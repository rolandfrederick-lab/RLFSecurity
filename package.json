{
  "name": "rlfsecurity",
  "private": true,
  "description": "R L Frederick Private Security & Weapon Safety website",
  "scripts": {
    "build": "esbuild src/home-scene.js --bundle --minify --format=esm --target=es2020 --outfile=public/js/home-scene.js"
  },
  "devDependencies": {
    "esbuild": "^0.28.2",
    "three": "^0.186.1"
  }
}
